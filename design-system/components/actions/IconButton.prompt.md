円形アイコンボタン(44px、ピル)。アイコンはユニコードグリフ(✻ → × ←)— このシステムにアイコンフォントは無い。

```jsx
<IconButton label="閉じる">&times;</IconButton>
<IconButton label="次へ" variant="outline">&rarr;</IconButton>
```

- `label` 必須(aria-label)
- hover は背景が bg-secondary に、150ms
